# Raw/local research evidence kept outside Git because repository hooks cap files at 500 KiB.
# Versioned MANIFEST/audit files retain identity, SHA-256, provenance, and conclusions.
**/*.pdf
**/*.png
**/events*.jsonl
**/summarize/claims/*.jsonl
**/summarize/paper_summaries.jsonl
local-corpus-*/python-deps/
local-corpus-*/package-cache/
engineering-validation/fixtures/**/*.eml
